{
  "_comment": "wiki-optimizer 프로젝트별 설정. install.sh 실행 시 .wiki/config.json으로 복사됩니다. 프로젝트 구조에 맞게 domainExtractors를 수정하세요.",
  "domainExtractors": [
    {
      "description": "Example: Spring Boot Java — Controller/Service/Mapper 등에서 모듈/도메인 추출",
      "regex": "src/main/java/.*?/(\\w+)/(\\w+?)(?:Controller|Service|Mapper|DTO|Form)?\\.java$",
      "domainTemplate": "$1/$2",
      "caseTransform": "kebab"
    },
    {
      "description": "Example: Frontend HTML/JS — 화면 파일에서 모듈/도메인 추출",
      "regex": "src/main/resources/.*?/(\\w+)/(\\w+)\\.(?:html|js)$",
      "domainTemplate": "$1/$2",
      "caseTransform": "kebab"
    }
  ]
}
